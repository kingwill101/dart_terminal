name: ghostty_uv_flutter_example
description: Example app for the ghostty_uv_flutter package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.10.4

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ghostty_vte_flutter: ^0.0.1
  ghostty_uv_flutter: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
